<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".splash.SplashScreenActivity">

    <LinearLayout
        android:id="@+id/layoutLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.499">

        <ImageView
            android:id="@+id/ivLogo"
            android:layout_width="130dp"
            android:layout_height="50dp"
            android:src="@drawable/img_logo_not_check" />

        <ImageView
            android:id="@+id/ivbBlankPayCheck"
            android:layout_width="261dp"
            android:layout_height="50dp"
            android:src="@drawable/img_blank_pay_check"
            android:translationX="1000dp" />

    </LinearLayout>

    <ImageView
        android:layout_marginTop="15dp"
        android:layout_marginStart="147dp"
        android:id="@+id/ivCheck"
        android:layout_width="27dp"
        android:layout_height="22dp"
        android:src="@drawable/img_check"
        app:layout_constraintStart_toStartOf="@+id/layoutLogo"
        app:layout_constraintTop_toTopOf="@+id/layoutLogo" />

</androidx.constraintlayout.widget.ConstraintLayout>